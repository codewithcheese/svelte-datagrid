name = "svelte-datagrid-docs"
compatibility_date = "2024-12-01"
compatibility_flags = ["nodejs_compat"]

# Build output directory for SvelteKit
# The adapter outputs to .svelte-kit/cloudflare
[build]
command = "npm run build"

# Assets configuration for the documentation site
[assets]
directory = ".svelte-kit/cloudflare"
